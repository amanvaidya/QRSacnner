This apllication is connecting with db using JTDS driver which is used too connect applicaiton with MSSQL database without using any web service.

In connection class change add the ip, port number, db name, username and password for your mssql server and change the tables where ever necessary.

This application is used to scan bar codes and qr codes and displaying pie chart at the time of login.

Application is using zinxg library for bar code scanning

Application includes flash light also.
